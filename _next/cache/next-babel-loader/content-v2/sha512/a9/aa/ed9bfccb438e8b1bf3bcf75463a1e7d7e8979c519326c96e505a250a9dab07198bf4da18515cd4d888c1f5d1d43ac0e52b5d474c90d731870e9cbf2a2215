{"ast":null,"code":"/**\n * 将数组等分割\n */\nexport const splitArray = (array, interval = 4) => {\n  const result = [];\n  let temp = [];\n\n  for (const item of array) {\n    temp.push(item);\n\n    if (temp.length === interval) {\n      result.push(temp);\n      temp = [];\n    }\n  }\n\n  if (temp.length) {\n    result.push(temp);\n  }\n\n  return result;\n};\nexport const getArray = array => Array.isArray(array) ? array : [];\n/**\n * 对象数组去重\n */\n\nexport const dedup = (array, compare) => array.reduce((acc, item) => acc.find(i => compare(i, item)) ? acc : acc.concat(item), []);","map":null,"metadata":{},"sourceType":"module"}